version: '3.9'

services:
  geo_teacher:
    build: .
    container_name: geo_teacher_llm
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
